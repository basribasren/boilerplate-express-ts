export interface {{camelCase name}}Interface {
	page?: string // Change to number if you prefer
	limit?: string // Change to number if you prefer
	id: string
	nama: string
	// Add any additional fields needed
}
